[workspace]
members = ["tinned_lib", "tinned_ffi", "tinned_py"]
default-members = ["tinned_lib"]
resolver = "3"

[profile.dev]
panic = "unwind"

[profile.release]
panic = "unwind"

[profile.release-ffi]
inherits = "release"
panic = "abort"
lto = true
codegen-units = 1
