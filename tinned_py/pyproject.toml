[build-system]
requires = ["maturin>=1.7"]
build-backend = "maturin"

[project]
name = "tinned"
version = "0.1.0"
requires-python = ">=3.13"

[tool.maturin]
module-name = "tinned"
manifest-path = "Cargo.toml"
